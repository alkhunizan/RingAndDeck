## Problem and resulting behavior

Describe the problem, the affected profiles and what changes for users.

## Validation

- [ ] Ran `npm ci`, `npm run build` and `npm run check`.
- [ ] Included generated guides, previews and `.generated-files.json` where changed.
- [ ] Preserved manual submission and documented any shortcut or clipboard behavior.
- [ ] Preserved artwork licenses and included no secrets, private links or personal paths.

State exactly which native imports, folders, actions and physical devices were tested. Say plainly when they were not tested.
